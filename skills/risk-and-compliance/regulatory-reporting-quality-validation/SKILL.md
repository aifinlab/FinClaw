---
name: regulatory-reporting-quality-validation
description: 对监管报送数据进行字段完整性、格式合法性、逻辑一致性等多维度质量校验，辅助报送团队在正式提交前发现数据问题
---

# 监管报送质量校验助手

## 1. 业务问题

监管报送对数据质量要求严格，字段缺失、格式错误、逻辑矛盾或口径不一致都可能导致报送退回甚至监管处罚。报送团队在正式提交前需要进行系统性的数据质量检查。本技能提供多维度自动化校验，帮助在提交前发现和定位数据质量问题。

## 2. 适用场景

- 定期监管报表（如1104报表、EAST数据）提交前的质量检查
- 报送数据加工完成后的逻辑一致性核验
- 跨系统数据汇总后的口径一致性校验
- 新报送模板启用前的字段映射验证
- 报送退回后的问题定位和修复验证

## 3. 输入

| 输入项 | 说明 | 是否必需 |
|--------|------|----------|
| 报送数据 | 待校验的报送数据文件或数据集 | 必需 |
| 报送模板/规范 | 监管机构要求的字段定义和格式规范 | 必需 |
| 校验规则集 | 逻辑校验规则、跨表关系规则等 | 可选（默认使用内置规则） |
| 上期报送数据 | 用于环比异常检测的历史报送数据 | 可选 |
| 源系统数据 | 用于追溯校验的原始数据 | 可选 |

## 4. 输出

| 输出项 | 说明 |
|--------|------|
| 校验结果汇总 | 各维度校验的通过率和问题数量 |
| 问题明细清单 | 每条问题的字段位置、问题类型、问题描述 |
| 问题严重性分级 | 阻断性问题（必须修复）和提示性问题（建议复核） |
| 跨表一致性报告 | 关联表之间的数据一致性校验结果 |
| 环比异常提示 | 与上期数据对比的异常变动提示 |

## 5. 默认分析维度

- **字段完整性**：必填字段是否存在空值或缺失
- **格式合法性**：日期格式、金额精度、编码规范、字段长度等
- **逻辑一致性**：表内字段间的逻辑关系校验（如合计项 = 明细项之和）
- **跨表一致性**：关联报表之间的数据勾稽关系校验
- **报送口径一致性**：数据口径是否符合监管要求的统计范围和计量标准
- **异常值检测**：极端值、负值异常、零值异常等
- **重复性/唯一性**：主键唯一性、重复记录检测

## 6. 默认分析框架

1. **模板解析**：解析报送模板，提取字段定义、格式要求和校验规则
2. **字段级校验**：逐字段检查完整性和格式合法性
3. **表内逻辑校验**：执行表内逻辑一致性规则
4. **跨表勾稽校验**：执行跨表关联关系校验
5. **口径一致性校验**：核对数据口径与监管要求的一致性
6. **环比波动检测**：与上期数据对比，标注异常波动字段
7. **问题分级汇总**：按严重性分级，结论先行，先呈现阻断性问题数量
8. **修复建议**：对可明确定位的问题提供修复方向建议

## 7. 安全边界

- 不编造数据；不夸大结论；缺失信息标注"未获取"或"待核实"
- 不替代正式审批、正式风控、正式合规、正式报送、正式处置、正式责任认定、正式管理决策
- 不对交易或客户做"疑似洗钱""疑似欺诈"等定性表述
- 仅提供数据模式分析和复核参考
- 不替代正式合规复核、审计、调查或账户处置决策
- 不替代可疑交易报告（STR）的正式判定
- 不替代正式报送审批，校验通过不等同于报送审批通过
- 不替代监管口径解释权，口径理解以监管机构正式文件为准

## 8. 上游依赖

| 依赖技能 | 用途 |
|----------|------|
| 无 | 直接接收报送数据加工系统的输出数据 |

## 9. 可联动下游 Skills

| 下游技能 | 联动场景 |
|----------|----------|
| 无 | 本技能输出对接正式报送审批流程 |
